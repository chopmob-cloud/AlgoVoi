name: Deploy GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - privacy-policy.html
      - site/**
      - .github/workflows/deploy-pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Build site
        working-directory: site
        # build:custom-domain uses cross-env VITE_BASE=/ so asset paths are
        # root-relative (/assets/...) rather than /AlgoVoi/assets/... which
        # 404 on the custom domain av.ilc-n.xyz.
        run: npm ci && npm run build:custom-domain

      - name: Copy root static files into site dist
        run: |
          cp privacy-policy.html site/dist/privacy-policy.html
          # CNAME tells GitHub Pages which custom domain owns this deployment.
          # Without it the custom domain is cleared on each deploy.
          echo "av.ilc-n.xyz" > site/dist/CNAME

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
